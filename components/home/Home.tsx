import React from 'react';
import { Text, View } from 'react-native';

const Home = (): React.ReactElement => {
  return (
    <View>
      <Text>Home</Text>
    </View>
  );
};

export default Home;
